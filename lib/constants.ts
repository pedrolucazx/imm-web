/**
 * Maximum number of AI requests allowed per day.
 */
export const AI_DAILY_LIMIT = 15;

/**
 * Minimum length required for user name.
 */
export const MIN_NAME_LENGTH = 2;

/**
 * Minimum length required for password.
 */
export const MIN_PASSWORD_LENGTH = 6;

/**
 * Maximum character length for habit name.
 */
export const MAX_HABIT_NAME_LENGTH = 80;

/**
 * Minimum available minutes for habit tracking.
 */
export const MIN_AVAILABLE_MINUTES = 5;

/**
 * Maximum available minutes for habit tracking.
 */
export const MAX_AVAILABLE_MINUTES = 120;

/**
 * Default available minutes when creating a new habit.
 */
export const DEFAULT_AVAILABLE_MINUTES = 30;

/**
 * Default mood score when creating a new journal entry.
 */
export const DEFAULT_MOOD_SCORE = 3;

/**
 * Default energy score when creating a new journal entry.
 */
export const DEFAULT_ENERGY_SCORE = 3;

/**
 * Maximum number of journal entries to retrieve in history pagination.
 */
export const JOURNAL_HISTORY_LIMIT = 100;
