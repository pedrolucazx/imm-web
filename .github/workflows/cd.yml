name: CD — Deploy (Vercel)

on:
  workflow_run:
    workflows: ["CI — imm-web"]
    types:
      - completed
    branches:
      - main
      - develop
  workflow_dispatch:

concurrency:
  group: deploy-web-${{ github.event.workflow_run.head_branch || github.ref_name }}
  cancel-in-progress: false

jobs:
  deploy:
    name: "Deploy to Vercel (${{ github.ref_name || github.event.workflow_run.head_branch }})"
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    environment:
      name: ${{ (github.ref_name || github.event.workflow_run.head_branch) == 'main' && 'production' || 'homolog' }}

    steps:
      - name: Validate secrets
        run: |
          BRANCH="${{ github.event.workflow_run.head_branch || github.ref_name }}"
          if [[ "$BRANCH" == "main" && -z "${{ secrets.VERCEL_PROJECT_ID }}" ]]; then
            echo "::error::VERCEL_PROJECT_ID is required for main branch"
            exit 1
          elif [[ "$BRANCH" == "develop" && -z "${{ secrets.VERCEL_PROJECT_ID_DEV }}" ]]; then
            echo "::error::VERCEL_PROJECT_ID_DEV is required for develop branch"
            exit 1
          elif [[ "$BRANCH" != "main" && "$BRANCH" != "develop" ]]; then
            echo "::error::Unsupported branch for CD: $BRANCH (allowed: main, develop)"
            exit 1
          fi

      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}

      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v41.1.4
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: >-
            ${{ (github.event.workflow_run.head_branch || github.ref_name) == 'main' && secrets.VERCEL_PROJECT_ID
              || (github.event.workflow_run.head_branch || github.ref_name) == 'develop' && secrets.VERCEL_PROJECT_ID_DEV
              || '' }}
          vercel-args: "--prod"
